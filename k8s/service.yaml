apiVersion: v1
kind: Service
metadata:
  name: portfolio-deployment
spec:
  selector:
    app: aboutmepage
  ports:
    - protocol: TCP
      port: 80        # Port exposed inside the cluster
      targetPort: 80  # Port your container listens on
  type: ClusterIP     # ✅ Don't include nodePort here
